pipeline{
    stages{
        stage('Build'){
            echo " Build the simple war file"
            sh 'date'
            sh 'pwd'
            sh 'echo $SHELL'
            sh 'echo $HOSTNAME'

        }
    }
}